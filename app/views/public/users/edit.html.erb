<h1>登録情報の編集</h1>

<%= form_with model: [:public,@user] do |f| %>
  <body>
    <tr>
      <th>ユーザー名（ニックネーム）</th>
      <td><%= f.text_field :name %></td>
    </tr>
    <tr>
      <th>メールアドレス</th>
      <td><%= f.text_field :email %></td>
    </tr>
    <%= f.submit "変更を保存" %>
  </body>
<% end %>